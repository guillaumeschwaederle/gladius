<div class="wrapper-page">
   <div class="text-center">
      <a href="index.html" class="logo-lg"><i class="mdi mdi-radar"></i> <span>GLADIUS</span> </a>
   </div>
   <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
   <div class="form-inputs">
      <%= f.input :email, required: false, autofocus: true %>
      <%= f.input :password, required: false %>
   </div>
   <div class="form-actions form-group text-center m-t-20">
      <div class="col-sm-12 text-center">
        <%= f.button :submit, "Se connecter", class:"btn btn-primary", type:"submit"%>
        <div class="col-sm-12">
        <hr>
      <%= link_to "Vous n'avez pas encore de compte, créez-en un ici !", new_registration_path(resource_name), class:"text-muted" %>
        </hr>
    </div>
      </div>
   </div>
<%= render "devise/shared/links" %>
   <% end %>
</div>
